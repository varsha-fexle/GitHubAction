name: Validate Changes
on:
  pull_request:
    types: [opened]
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: "16.x"

      - name: Install Dependencies
        run: |
          npm install sfdx-cli --global
          echo y | sfdx plugins:install sfdx-git-delta
          sfdx plugins

      - name: 'Populate auth file with DEVHUB_SFDX_URL secret'
        run: |
          echo ${{ secrets.DEVHUB_SFDX_URL }} > ./DEVHUB_SFDX_URL.txt
          secretFileSize=$(wc -c "./DEVHUB_SFDX_URL.txt" | awk '{print $1}')
          if [ $secretFileSize == 1 ]; then
            echo "Missing DEVHUB_SFDX_URL secret. Is this workflow running on a fork?";
            exit 1;
          fi

      - name: Authenticate Dev Hub
        run: |
          sfdx force:auth:sfdxurl:store -f ./DEVHUB_SFDX_URL.txt -d -a devhub

      - name: Retrieve changed source
        run: |
          sfdx force:source:pull --forceoverwrite --json -u devhub

      - name: Find package.xml
        id: find_package
        run: |
          package_xml=$(find ./ -type f -name "package.xml" | head -n 1)
          echo "::set-output name=package_xml::$package_xml"

      - name: Validate changes
        run: |
          sfdx force:source:deploy --checkonly \
            --manifest=${{ steps.find_package.outputs.package_xml }} \
            --testlevel RunLocalTests \
            -u devhub \
            --wait 200

